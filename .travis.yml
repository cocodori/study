language : java
jdk :
  -openjdk11

branches:
  only:
    master

# Travis CI서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$Home/.gradle'

script: "./gradlew clean build"

# CI 실행 완료 시 메일로 알람
notifications :
  email:
    recipients:
      - 'leejihun1024@gamil.com'